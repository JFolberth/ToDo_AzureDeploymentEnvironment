resources:
  repositories:
  - repository: templates
    type: github
    name: JFolberth/TheYAMLPipelineOne
    endpoint: JFolberth
    ref: feature/tf_dotnet
trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

stages:
    - template: demo_environment_template_tf.yml